# @wtrn/rpc-contract

Shared oRPC contract and Zod schemas for the API, web client, and runner.

This package defines the typed boundary for auth status, runner registration,
runner listing, fake sandbox starts, and runner heartbeats.
